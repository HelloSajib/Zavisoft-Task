/// Represents the state of the internet connection.
enum InternetConnection {connected, disconnected}

/// Represents the status of an asynchronous operation (e.g., fetching data).
enum Status {initial, loading, success, failure, error}

/// Defines the available ad networks for monetization.
enum AdNetwork {admob, meta, none}
